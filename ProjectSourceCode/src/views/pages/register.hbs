<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register</title>
  <style>
    body {
      background-color: white;
      font-family: 'Gowun Batang', serif;
    }
    .register-container {
      background-color: #f9f9f9;
      color: black;
      max-width: 500px;
      margin: 40px auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 8px;
    }
    input, select {
      width: 100%;
      padding: 8px;
      margin-top: 5px;
      border-radius: 4px;
      border: 1px solid #ccc;
      font-family: 'Gowun Batang', serif;
    }
    button {
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 4px;
      padding: 10px 20px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="register-container">
    <h1 style="text-align: center;">Register</h1>

    {{#if message}}
    <div style="background-color: #f8d7da; padding: 10px; border-radius: 4px;">{{message}}</div>
    {{/if}}

    {{#if success}}
    <div style="background-color: #d4edda; padding: 10px; border-radius: 4px;">{{success}}</div>
    {{/if}}

    <form action="/register" method="POST">
      <label for="accountType"><strong>Account Type</strong></label>
      <select name="accountType" required>
        <option value="">Select account type</option>
        <option value="user">Artist</option>
        <option value="client">Client</option>
      </select>

      <label for="username"><strong>Username</strong></label>
      <input type="text" name="username" required>

      <label for="email"><strong>Email Address</strong></label>
      <input type="email" name="email" required>
      <div style="font-size: 0.9em;">We’ll never share your email.</div>

      <label for="password"><strong>Password</strong></label>
      <input type="password" name="password" required>

      <label for="confirmPassword"><strong>Confirm Password</strong></label>
      <input type="password" name="confirmPassword" required>

      <input type="hidden" name="isClientHidden" value="false">

      <div style="text-align: center; margin-top: 15px;">
        <button type="submit">Register</button>
      </div>
    </form>

    <p style="text-align: center; margin-top: 15px;">Already have an account? <a href="/login">Login here</a></p>
  </div>
</body>
</html>
