{{> navbar}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Art Feed</title>
</head>
<body>
  <div class="container" style="max-width: 800px; margin: 40px auto;">
    <h1 style="text-align: center;">Art Feed</h1>

    {{#if posts.length}}
      {{#each posts}}
        <article class="post" style="margin-bottom: 40px; padding: 20px; border: 1px solid #ccc; border-radius: 6px;">
          <h2>{{title}}</h2>
          <p><strong>By:</strong> {{username}}</p>
          <p><strong>Category:</strong> {{category}}</p>
          <p><strong>Date Posted:</strong> {{date_created}}</p>
          <p>{{description}}</p>
        </article>
      {{/each}}
    {{else}}
      <p style="text-align: center;">No posts yet.</p>
    {{/if}}
  </div>
</body>
</html>
